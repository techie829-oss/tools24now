# Backend Environment Variables
# Copy this file to backend/.env and update with your production values

# Application Settings
PROJECT_NAME=PDF Tools Platform
API_V1_STR=/api/v1
SECRET_KEY=your-super-secret-key-change-this-in-production-min-32-chars

# Database Configuration
# Uncomment ONE of the following based on your database choice:

# Option 1: MySQL (connects to external mysql_default network - recommended for production)
DATABASE_URL=mysql://pdftools_user:your-password@mysql/pdftools_db

# Option 2: PostgreSQL
# DATABASE_URL=postgresql://pdftools_user:your-password@postgres/pdftools_db

# Option 3: SQLite (for development/testing only)
# DATABASE_URL=sqlite:///./data/app.db

# CORS Origins (comma-separated, update with your frontend domain)
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# MySQL Connection Details (if using MySQL)
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=pdftools_db
MYSQL_USER=pdftools_user
MYSQL_PASSWORD=your-password

# PostgreSQL Connection Details (if using PostgreSQL)
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_DB=pdftools_db
# POSTGRES_USER=pdftools_user
# POSTGRES_PASSWORD=your-password
