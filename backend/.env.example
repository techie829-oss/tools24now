# Backend Environment Variables
# Copy this file to backend/.env and update with your production values

# Application Settings
PROJECT_NAME=PDF Tools Platform
API_V1_STR=/api/v1
SECRET_KEY=your-super-secret-key-change-this-in-production-min-32-chars
ALLOWED_HOSTS=["tools24now.site", "app.tools24now.site", "localhost"]
ALLOWED_ORIGINS=["https://tools24now.site", "http://localhost:3000"]

# Database Configuration
# You can use either DATABASE_URL (overrides everything) OR the generic variables below.

# ==========================================
# Database Configuration Examples (Local Development)
# ==========================================

# --- 1. PostgreSQL (Local) ---
# Option A: Full URL
# DATABASE_URL=postgresql://postgres:password@127.0.0.1:5432/tools24now

# Option B: Component Variables
# DB_TYPE=postgresql
# DB_HOST=127.0.0.1
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=password
# DB_NAME=tools24now

# --- 2. MySQL (Local) ---
# Option A: Full URL
# DATABASE_URL=mysql://root:password@127.0.0.1:3306/tools24now

# Option B: Component Variables
# DB_TYPE=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=password
# DB_NAME=tools24now

# --- 3. SQLite (No Setup Required) ---
DB_TYPE=sqlite
DB_NAME=./data/app.db
# (Host, User, Password, Port are ignored for SQLite)


# Option 2: PostgreSQL
# DATABASE_URL=postgresql://user:password@postgres/dbname

# Option 3: SQLite (for development/testing only)
# DATABASE_URL=sqlite:///./data/app.db
